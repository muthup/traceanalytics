receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:55680
        
  exporters:
  otlp/2:
    endpoint:
    tls:
      insecure: true
  logging:

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [logging, otlp/2]
